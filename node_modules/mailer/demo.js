var email = require("./lib/node_mailer");

for(var i = 0; i < 1; i++){
  email.send({
    host : "smtp.sendgrid.net",              // smtp server hostname
    port : "587",                     // smtp server port
    domain : "madewithnuance.com",            // domain used by client to identify itself to server
    to : "christian@madewithnuance.com",
    from : "howdy@madewithnuance.com",
    subject : "node_mailer test email",
    body: "Howdy, nuance crew.",
    authentication : "login",        // auth login is supported; anything else is no auth
    username : "madewithnuance",            // username
    password : "wearenuance",            // password
    debug: true                      // log level per message
  },
  function(err, result){
    if(err){ console.log(err); }
  });
}